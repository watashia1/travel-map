# 旅行足迹 / 路线地图生成器
## Product & Technical Specification

版本：V1.0  
目标：开发一个可直接部署到 **GitHub Pages** 的轻量中文地图路线生成工具。  
产品定位：**一个“懂经纬度”的轻量地图视觉编辑器。**

---

## 1. 项目目标

开发一个纯前端单页网页工具。

用户打开网页后，可以通过：

- 输入地名；
- 输入经纬度；
- 地名与经纬度混合输入；

快速生成一张旅行路线地图。

地图自动完成：

> 地点定位 → 红点标记 → 地名显示 → 按输入顺序连线 → 用户拖动微调 → 导出 PNG / SVG

整个核心功能必须运行在浏览器端。

### 1.1 核心约束

第一版必须做到：

- 无服务器；
- 无数据库；
- 无登录；
- 无 API Key；
- 不强依赖任何在线 API；
- 可部署到 GitHub Pages；
- 用户旅行数据默认不上传；
- 尽量支持离线使用。

不要把项目做成 GIS 软件，也不要做成旅行规划平台。

---

## 2. 典型使用场景

例如用户想制作：

**东京 → 札幌 → 奥斯陆 → 雷克雅未克 → 朗伊尔城**

输入：

```text
东京
札幌
奥斯陆
雷克雅未克
朗伊尔城
```

系统自动解析地点并生成地图。

也可以输入：

```text
35.6762, 139.6503 | 东京
43.0618, 141.3545 | 札幌
59.9139, 10.7522 | 奥斯陆
64.1466, -21.9426 | 雷克雅未克
78.2232, 15.6469 | 朗伊尔城
```

这种情况下不需要任何地名搜索，直接根据经纬度绘制。

还允许混合：

```text
东京
札幌
59.9139, 10.7522 | 奥斯陆
雷克雅未克
78.2232, 15.6469 | 朗伊尔城
```

---

## 3. 最重要的产品原则

### 3.1 经纬度优先

系统内部所有地点最终统一为：

```javascript
{
  name: "东京",
  lat: 35.6762,
  lon: 139.6503
}
```

地名只是获取经纬度的一种输入方式。

经纬度才是系统内部真正可靠的数据。

---

### 3.2 坐标输入不需要 API

例如：

```text
64.1466, -21.9426 | 雷克雅未克
```

必须完全在浏览器本地解析。

不得请求任何服务器。

---

### 3.3 地名搜索优先走本地数据库

例如：

```text
东京
```

首先搜索：

```text
places.json
```

找到以后直接返回经纬度。

只有本地数据库无法识别时，才进入异常流程。

---

### 3.4 在线搜索只是可选增强

第一版核心功能不依赖：

- Google Maps；
- 高德；
- 百度；
- Nominatim；
- Mapbox。

以后可以增加：

```text
在线搜索此地点
```

但在线 API 失败时，工具仍必须能够正常使用。

---

## 4. 用户输入设计

主界面只保留一个核心输入区域。

例如：

```text
输入地点或坐标
────────────────────

东京
札幌
59.9139, 10.7522 | 奥斯陆
64.1466, -21.9426 | 雷克雅未克
78.2232, 15.6469 | 朗伊尔城

────────────────────

[生成地图]
```

一行代表一个地点。

输入顺序就是路线顺序。

---

## 5. 正式支持的输入格式

第一版不要过度“智能猜测”。

重点支持三类。

### 形式 A：地名

```text
东京
```

### 形式 B：经纬度

```text
35.6762, 139.6503
```

没有名字时，可以显示：

```text
地点 1
```

### 形式 C：经纬度 + 名称

```text
35.6762, 139.6503 | 东京
```

规定：

> 用户输入始终采用  
> **纬度, 经度 | 名称**

即：

```text
Lat, Lon | Label
```

代码内部再转换为地图库需要的 `[lon, lat]`。

---

## 6. 坐标格式

建议直接使用：

**FacilMap / parse-gps-coordinates**

GitHub：

```text
https://github.com/FacilMap/parse-gps-coordinates
```

它已经支持大量现实中的坐标表达方式，包括：

- 十进制度；
- 度分秒；
- 方向字母；
- `geo:` URI；
- Unicode 负号；
- 多种分隔符。

例如：

```text
35.6762, 139.6503
```

以及：

```text
35°40'34"N, 139°39'1"E
```

不要自己重新写复杂坐标正则。

---

## 7. 地名解析

建立一个本地：

```text
public/data/places.json
```

每个地点可以类似：

```json
{
  "name": "Tokyo",
  "displayName": "东京",
  "aliases": [
    "东京",
    "東京都",
    "Tokyo"
  ],
  "lat": 35.6762,
  "lon": 139.6503,
  "country": "日本"
}
```

搜索：

```text
东京
Tokyo
東京都
```

都返回同一个位置。

---

## 8. 本地地点库

第一版不需要试图收录全世界所有地点。

目标：

**5,000～20,000 个常用城市 / 地点。**

优先覆盖：

- 世界主要城市；
- 国家首都；
- 旅游城市；
- 中国主要城市；
- 日本主要城市；
- 欧洲主要城市；
- 岛屿；
- 极地主要地点。

数据源可以考虑：

- GeoNames；
- Natural Earth；
- 其他兼容许可证的公开地名数据。

开发时必须检查许可证和 attribution 要求。

不要把许可不清的数据直接提交仓库。

---

## 9. 地名无法识别时

不得偷偷猜。

地点列表显示：

```text
东京              ✓
札幌              ✓
奥斯陆            ✓
某北极观察点       !
```

点击异常项后：

```text
没有找到“某北极观察点”

[输入经纬度]
[在地图上指定位置]
```

第二阶段可以增加：

```text
[在线搜索]
```

---

## 10. 地图

### 10.1 第一版优先支持内置地图

至少提供：

- 世界地图；
- 欧洲；
- 亚洲；
- 中国；
- 日本；
- 北极。

第一版最重要的是：

**世界地图。**

地图使用：

```text
GeoJSON / TopoJSON
+
D3 Geo
+
SVG
```

不使用 Google Maps / 在线瓦片作为核心地图。

---

## 11. 默认视觉

建议默认世界地图：

- 浅灰或米白大陆；
- 白色或透明背景；
- 很细的国界；
- 无国家名称；
- 无道路；
- 无 POI；
- 无复杂地图元素。

目的不是导航。

而是作为：

**旅行路线视觉底板。**

---

## 12. 地图投影

默认：

**Equal Earth**

或者：

**Natural Earth**

理由是视觉上比 Mercator 更适合世界旅行线路展示。

同时提供：

```text
地图投影

○ Equal Earth
○ Natural Earth
○ Mercator
```

第一版不要提供十几种投影。

---

## 13. Marker

默认：

```text
● 东京
```

Marker：

- 红色；
- 实心圆；
- 直径约 8px。

允许调整：

- 颜色；
- 大小；
- 边框。

支持：

```text
● 东京
```

以及：

```text
① 东京
```

---

## 14. Label

每个 Marker 有一个标签。

例如：

```text
● 东京
```

标签必须支持鼠标拖动。

拖动标签：

**只改变文字位置，不改变真实地点。**

内部存储：

```javascript
labelOffsetX
labelOffsetY
```

---

## 15. Marker 微调

Marker 本身也可以拖动。

但是：

拖动 Marker 之后，不修改经纬度。

保存：

```javascript
autoX
autoY
manualOffsetX
manualOffsetY
```

并提供：

```text
恢复自动位置
```

---

## 16. 路线

默认按照输入顺序自动连线。

例如：

```text
东京
↓
札幌
↓
奥斯陆
↓
雷克雅未克
```

地图生成：

```text
东京 → 札幌 → 奥斯陆 → 雷克雅未克
```

---

## 17. 路线样式

至少提供：

### 直线

```text
A ─── B
```

### 平滑曲线

推荐默认：

```text
A ╭─── B
```

可以调整：

- 颜色；
- 粗细；
- 透明度；
- 实线 / 虚线。

---

## 18. 地点排序

地点列表：

```text
☰ ① 东京
☰ ② 札幌
☰ ③ 奥斯陆
☰ ④ 雷克雅未克
```

支持拖动排序。

排序以后：

**线路立即重新生成。**

不要要求用户删除再重新输入。

---

## 19. 编辑体验

必须支持：

```text
Ctrl + Z
```

撤销。

以及：

```text
Ctrl + Shift + Z
```

或：

```text
Ctrl + Y
```

重做。

至少包括：

- 移动标签；
- 移动 Marker；
- 删除地点；
- 排序；
- 样式修改。

---

## 20. 画布技术

推荐：

**SVG**

不要第一版做复杂 Canvas 编辑器。

页面结构可类似：

```html
<div id="map">
  <svg>
    <g id="countries"></g>
    <g id="routes"></g>
    <g id="markers"></g>
    <g id="labels"></g>
  </svg>
</div>
```

SVG天然适合：

- 点；
- 文字；
- Path；
- 拖拽；
- 缩放；
- SVG导出。

---

## 21. 推荐技术栈

优先追求轻量。

推荐：

```text
HTML
CSS
JavaScript / TypeScript
D3 Geo
SVG
```

可以使用 Vite：

```text
Vite + TypeScript
```

但不要为了项目规模引入：

- Next.js；
- Node 后端；
- SQLite；
- Docker；
- PostgreSQL；
- Firebase；
- 用户系统。

---

## 22. 是否需要 React

**不是必须。**

如果开发 AI 能用原生 TypeScript 清晰实现：

优先原生。

如果使用 React 能明显提高代码维护性，也允许：

```text
React
+
TypeScript
+
Vite
```

但不得因此引入服务端。

---

## 23. 推荐项目目录

```text
travel-map/
│
├── index.html
├── package.json
├── vite.config.ts
│
├── src/
│   ├── main.ts
│   ├── app.ts
│   │
│   ├── map/
│   │   ├── renderer.ts
│   │   ├── projection.ts
│   │   ├── routes.ts
│   │   └── markers.ts
│   │
│   ├── input/
│   │   ├── parser.ts
│   │   ├── coordinates.ts
│   │   └── placeSearch.ts
│   │
│   ├── editor/
│   │   ├── drag.ts
│   │   ├── history.ts
│   │   └── labels.ts
│   │
│   ├── export/
│   │   ├── svg.ts
│   │   └── png.ts
│   │
│   └── styles.css
│
├── public/
│   └── data/
│       ├── world.json
│       └── places.json
│
└── README.md
```

如果使用原生单文件方案，也可以进一步缩小。

---

## 24. 数据结构

地点：

```typescript
interface Place {
  id: string

  name: string

  lat: number
  lon: number

  source:
    | 'coordinates'
    | 'local-place-db'
    | 'manual'

  order: number

  markerOffset?: {
    x: number
    y: number
  }

  labelOffset?: {
    x: number
    y: number
  }
}
```

---

## 25. 输入解析流程

每一行：

```text
raw input
```

执行：

```text
是否包含坐标？
      │
      ├── YES
      │
      ↓
  parse coordinate
      ↓
  获取可选 Label
      ↓
  创建 Place
      │
      └────────────→ 完成

      NO
      ↓

搜索本地 places.json
      │
      ├── 找到唯一结果 → 创建 Place
      │
      ├── 多个结果 → 用户选择
      │
      └── 找不到 → 异常状态
```

---

## 26. 地名歧义

例如：

```text
Cambridge
```

可能对应多个地点。

不能静默选择。

显示：

```text
Cambridge

○ Cambridge, England
○ Cambridge, Massachusetts, USA
○ Cambridge, Ontario, Canada
```

---

## 27. 用户数据

所有编辑数据默认只保存在浏览器。

第一阶段使用：

```text
localStorage
```

即可。

例如保存：

```text
最近项目
```

后续如果项目数据变大再换 IndexedDB。

---

## 28. 项目保存

建议支持：

```text
导出项目
```

生成：

```text
my-trip.json
```

以后：

```text
导入项目
```

恢复全部：

- 地点；
- 顺序；
- 经纬度；
- Marker 调整；
- 标签位置；
- 路线；
- 样式。

---

## 29. 导出 SVG

必须支持。

输出内容：

```text
地图
+
路线
+
Marker
+
Label
```

SVG建议保留文字为 `<text>`。

不要第一版做字体转曲。

---

## 30. 导出 PNG

必须支持：

```text
1×
2×
4×
```

以及：

```text
自定义宽度
```

例如：

```text
6000 px
```

保证适合：

- 海报；
- 图书；
- PPT；
- 微信推文；
- 社交媒体。

---

## 31. 透明导出

建议加入：

```text
仅导出路线图层
```

背景透明。

内容：

```text
Marker
+
Label
+
Route
```

这样可以直接覆盖到：

- Photoshop；
- Figma；
- Illustrator；
- 海报底图。

---

## 32. 自定义底图

不要第一版支持：

> 任意上传一张图片，然后系统自动识别它是什么地图。

这是另一个复杂项目。

### V1.1 可以增加简单模式

上传：

**标准等距圆柱世界地图**

要求满足：

```text
左边 = -180°
右边 = +180°
上边 = +90°
下边 = -90°
```

这样可以直接计算位置。

不做地图校准。

---

## 33. 任意插画地图

例如：

- 手绘世界地图；
- 极区插画；
- 岛屿人为放大；
- 地图变形。

放到：

**V2**

实现方式才考虑：

```text
人工校准
```

而不是让 AI 猜。

---

## 34. UI 布局

桌面优先。

建议：

```text
┌─────────────────────────────────────────┐
│ 旅行地图                       导出 ▾   │
├────────────┬────────────────────────────┤
│            │                            │
│ 地图       │                            │
│            │                            │
│ 地点       │          地图              │
│            │                            │
│ 样式       │                            │
│            │                            │
│ 项目       │                            │
│            │                            │
└────────────┴────────────────────────────┘
```

左侧约：

```text
280–340px
```

中间尽量给地图。

---

## 35. 左侧功能

### 地图

```text
世界
欧洲
亚洲
中国
日本
北极
```

### 地点

输入框 + 地点列表。

### 样式

Marker、文字、路线设置。

### 项目

保存、导入、清空。

---

## 36. 不要做成“开发者工具”

普通用户不应该看到：

```text
EPSG:4326
GeoJSON
projection scale
transform matrix
```

这些属于内部实现。

UI只显示：

```text
地图
地点
路线
样式
导出
```

---

## 37. GitHub Pages 部署

项目必须兼容：

```text
GitHub Pages
```

最终用户访问：

```text
https://USERNAME.github.io/travel-map/
```

即可直接使用。

如果用 Vite：

必须正确配置：

```typescript
base: '/travel-map/'
```

或者根据 repository 自动处理 base path。

---

## 38. 自动部署

建议配置：

```text
.github/workflows/deploy.yml
```

push 到：

```text
main
```

以后自动：

```text
build
↓
deploy GitHub Pages
```

用户无需手动上传 `dist`。

---

## 39. PWA

不属于第一版硬性要求。

可以在 V1.1 加：

```text
manifest.json
service-worker.js
```

实现：

- 安装到桌面；
- 缓存核心资源；
- 基础离线使用。

---

## 40. 明确禁止过度开发

第一版不要开发：

- 用户账号；
- 云同步；
- AI；
- MCP；
- 后端；
- 数据库；
- Docker；
- 实时导航；
- 公交路线；
- 酒店推荐；
- 航班查询；
- GPS 实时定位；
- 3D 地球；
- 多人协作；
- 照片管理；
- 社交功能。

这些与核心目标无关。

---

## 41. GitHub 参考项目

### 41.1 ng-simple-maps

重点研究：

- D3 Geo；
- 经纬度 Marker；
- Annotation；
- curved line；
- 多种投影；
- SVG 地图。

GitHub：

```text
https://github.com/hanafnafs/ng-simple-maps
```

不要照搬 Angular 架构。

只参考地图实现。

---

### 41.2 GPX Route Editor

重点研究：

- 浏览器端运行；
- 无后端；
- Map 编辑；
- 点拖动；
- Undo / Redo；
- 单页工具思路。

GitHub：

```text
https://github.com/stealth-factory/gpx-route-editor
```

---

### 41.3 MapAnNai Plus

重点研究：

- Trip；
- Marker；
- drag reorder；
- polyline；
- 地点与路线组织方式。

GitHub：

```text
https://github.com/RicterZ/mapannai-plus
```

但不要采用它的复杂架构。

它使用 Next.js、SQLite、Google Places、腾讯云 COS、MCP 等，对本项目明显过重。

---

### 41.4 parse-gps-coordinates

用于解析各种现实输入坐标格式。

GitHub：

```text
https://github.com/FacilMap/parse-gps-coordinates
```

---

## 42. MVP 功能清单

第一版只要求以下功能完整可用：

1. 世界地图；
2. 输入地名；
3. 输入经纬度；
4. 地名与经纬度混合输入；
5. 本地地点数据库搜索；
6. Marker；
7. 地名标签；
8. 按输入顺序连线；
9. 拖动地点排序；
10. 拖 Marker；
11. 拖 Label；
12. 路线样式；
13. Marker 样式；
14. Undo；
15. Redo；
16. 保存项目；
17. 打开项目；
18. PNG 导出；
19. SVG 导出；
20. GitHub Pages 部署。

---

## 43. 第一版验收案例

输入：

```text
东京
43.0618, 141.3545 | 札幌
奥斯陆
64.1466, -21.9426 | 雷克雅未克
78.2232, 15.6469 | 朗伊尔城
```

期望：

地图自动出现五个地点。

其中：

```text
东京
奥斯陆
```

来自本地地名库。

另外三个：

直接来自经纬度。

路线必须严格按：

```text
东京
→
札幌
→
奥斯陆
→
雷克雅未克
→
朗伊尔城
```

连接。

---

## 44. 交互验收

用户必须可以：

拖动：

```text
朗伊尔城
```

标签，而不改变红点。

然后拖动红点进行视觉微调。

再：

```text
Ctrl + Z
```

恢复。

---

## 45. 数据验收

刷新网页以后：

最近编辑项目仍可恢复。

导出：

```text
trip.json
```

重新导入后，应与保存前一致。

---

## 46. 输出验收

### SVG

放大后：

- 地图不模糊；
- 点不模糊；
- 线路不模糊；
- 文字保持可编辑。

### PNG

6000px 导出后：

- 地图完整；
- 无裁切；
- 标签位置与网页一致；
- 中文正常。

---

## 47. 性能目标

普通电脑：

```text
100 个地点
```

仍应保持流畅。

但主要优化目标仍然是：

```text
5～50 个地点
```

这是实际典型场景。

---

## 48. 开发顺序

不要先做漂亮 UI。

严格按下面执行：

```text
① 经纬度 → SVG 坐标
② 世界地图显示
③ Marker
④ 地名输入
⑤ 坐标输入
⑥ 本地点名数据库
⑦ Route
⑧ 排序
⑨ Marker / Label 拖动
⑩ Undo / Redo
⑪ SVG 导出
⑫ PNG 导出
⑬ 项目保存
⑭ UI 整理
⑮ GitHub Pages 部署
```

只要：

> 经纬度 → 地图位置

这一核心链路没有通过测试，

不要继续堆功能。

---

## 49. Definition of Done

只有下面这条完整链路可以稳定执行，才算 MVP 完成：

```text
打开 GitHub Pages 网页
↓
输入 5～20 个地名 / 经纬度
↓
系统正确识别
↓
自动生成红点和名称
↓
按照顺序自动连线
↓
拖动地点排序
↓
线路实时改变
↓
拖动红点和文字微调
↓
Ctrl + Z 正常撤销
↓
导出高清 PNG
↓
导出 SVG
↓
刷新浏览器
↓
项目仍可以恢复
```

否则项目未完成。

---

# 50. 给开发 AI 的最终执行指令

请根据上述规格完整实现项目。

第一原则是保持轻量，不要主动增加后端、数据库、账号系统、AI 服务、在线地图 API 或其他未要求功能。

项目必须可以静态部署到 GitHub Pages。

第一版所有核心功能必须仅依赖浏览器运行。

请先实现并测试最小闭环：

> **经纬度输入 → 世界地图正确定位 → 红点 → 标签 → 按顺序连线 → PNG / SVG 导出。**

之后再实现：

- 地名数据库；
- 拖动编辑；
- 排序；
- Undo / Redo；
- 项目保存。

如果实现过程中发现需求存在技术冲突，不要自行扩大架构，而应优先寻找更简单的实现方案。

最终请同时提供：

- 完整源代码；
- README；
- 本地运行说明；
- GitHub Pages 部署配置；
- 示例数据；
- 测试说明；
- 已知限制；
- 第一次部署步骤。

不要只生成原型界面。

必须交付一个可以实际使用、编辑、保存和导出的完整工具。

---

# 51. 开发优先级总结

## P0：必须先完成

```text
世界地图
+
经纬度输入
+
SVG Marker
+
Label
+
Route
+
PNG / SVG 导出
```

## P1：完成后再加

```text
本地地名库
+
地名输入
+
拖动排序
+
Marker / Label 拖动
+
Undo / Redo
+
项目保存
```

## P2：以后再考虑

```text
在线地名搜索
PWA
更多区域地图
透明图层导出
自定义底图
```

## P3：V2 以后

```text
任意插画地图校准
复杂投影
多路线
时间轴
动画路线
互动 HTML 输出
```

---

# 52. 一句话产品定义

> **输入地名或经纬度，自动生成可编辑、可连线、可导出的旅行足迹地图。**
